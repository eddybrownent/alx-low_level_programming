#include "main.h"

/**
* _abs - computes the absolute value of an integer
* @v: the integer to be computed
* Return: the absolute value
*/
int _abs(int v)
{
if (v >= 0)
return (v);
else
return (-v);
}
